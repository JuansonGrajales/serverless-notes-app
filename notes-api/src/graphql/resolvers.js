const resolvers = {
    Query: {
      // getNote(_, args) {},
      // getNotes(_, args) {},
      // searchNotes(_, args) {},
    },
    Mutation: {
      // createNote(_, args) {},
      // updateNote(_, args) {},
      // deleteNote(_, args) {},
      // deleteNotes(_, args) {},
    },
  };
  
  module.exports = resolvers;
  